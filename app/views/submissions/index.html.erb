<% content_for :header do %>
  <h1>All Submissions</h1>
<% end %>

<ul id="grouped_submissions">
  <% @grouped_submissions.each do |conference, submissions| %>
    <li class="conference">
      <hgroup>
        <h1><%= conference.name %></h1>
        <h2>
          <%= conference.start_date.strftime("%b %d, %Y") %> &mdash; <%= conference.end_date.strftime("%b %d, %Y") %>
        </h2>
      </hgroup>
      <ul>
        <% for submission in submissions %>
          <li><%= link_to submission.title, submission %> | <%= link_to 'Download PDF', submission_path(submission, :format => "pdf") %></li>
        <% end %>
      </ul>
    </li>
  <% end %>
</ul>